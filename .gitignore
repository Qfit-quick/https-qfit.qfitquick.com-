node_modules/
.env
.env.*
!.env.example
*.log
screenshots/
.vite/
# wrangler 로컬 개발 전용(2026-09-25) — .dev.vars 는 로컬 시크릿,
# .wrangler/ 는 miniflare 로컬 캐시(sqlite 등)라 커밋할 이유가 없다.
# 한 번 실수로 커밋된 적이 있어(dev 브랜치) 여기 명시해 둔다.
.dev.vars
.wrangler/
